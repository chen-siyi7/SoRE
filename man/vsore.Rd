% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vsore.R
\name{vsore}
\alias{vsore}
\title{V-SoRE: variational empirical-Bayes fine-mapping with annotation ranking}
\usage{
vsore(z, R, anchor_weights, n, L = 10, K0 = 20, T_max = 15,
      tol = 1e-3, eps = 1e-6, n_inner = 100, verbose = FALSE)
}
\arguments{
\item{z}{Numeric vector of marginal z-scores, length p.}
\item{R}{p-by-p LD matrix (ridge-regularized reference-panel estimate).}
\item{anchor_weights}{Numeric vector of length p; external annotation anchor weights. Set all entries to 1 to recover SuSiE-RSS.}
\item{n}{Effective sample size for the SuSiE-RSS call.}
\item{L}{Maximum number of single-effect components (default 10).}
\item{K0}{Top-K0 variants by anchor weight used for the Spearman diagnostic (default 20).}
\item{T_max}{Maximum number of outer iterations (default 15).}
\item{tol}{Outer-loop convergence tolerance on log-weight change (default 1e-3).}
\item{eps}{PIP clipping value to avoid log(0) (default 1e-6).}
\item{n_inner}{Inner SuSiE-RSS iterations per outer pass (default 100).}
\item{verbose}{Logical; print progress at each outer iteration.}
}
\value{
A list with elements \code{pip}, \code{tau2}, \code{weights}, \code{n_iter}, \code{converged}, and \code{susie_fit}.
}
\description{
Runs the V-SoRE outer reweighting loop around SuSiE-RSS. The outer loop
alternates between estimating the annotation-data agreement parameter
tau^2 from the Spearman rank correlation between the anchor weights and
the current posterior inclusion probabilities, and re-running SuSiE-RSS
with updated weights.
}
\examples{
\dontrun{
set.seed(1)
p <- 200; n <- 50000
R <- 0.9^abs(outer(1:p, 1:p, "-"))
causal <- c(50, 150)
b <- numeric(p); b[causal] <- 5
z <- as.numeric(R \%*\% b + crossprod(chol(R), rnorm(p)))
omega <- rep(1, p); omega[causal] <- 100
fit <- vsore(z, R, omega, n = n)
}
}
